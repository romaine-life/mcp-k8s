# mcp-k8s

This repo is a standalone split from `romaine-life/tank-operator`. Keep the MCP API compact by default: list/discovery tools should accept filters and limits, and full raw payloads should require explicit opt-in parameters.
